# CUDA Parallel Adding Program
CUDA based simple program to perform parallel adding operations.

It uses multiple threads on GPU to significantly speed up mathematical operation of adding vast collection of numbers.
